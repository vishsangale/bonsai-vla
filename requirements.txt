# Pinned: torch and torchvision must be version-matched with each other
torch==2.11.0
torchvision==0.26.0

# Minimum version: stable APIs, safe to upgrade within major version
hydra-core>=1.3.2,<2.0
pytest>=9.0.3,<10.0
manim>=0.20.1,<1.0
einops>=0.8.2
matplotlib>=3.10.8,<4.0
tensorboard>=2.20.0
